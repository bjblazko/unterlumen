{
  "name": "unterlumen-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "bash fixtures/setup.sh",
    "test": "bash fixtures/setup.sh && playwright test",
    "test:headed": "bash fixtures/setup.sh && playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
